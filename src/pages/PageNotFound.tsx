import { Typography } from "@mui/material"

const PageNotFound = () => {
    return (
      <Typography>
        {'Oops, this page does not exist.'}
      </Typography>
    )
  }
  
  export default PageNotFound